import { DeviationValidators } from './deviation-validators';

describe('DeviationValidators', () => {
  it('should create an instance', () => {
    expect(new DeviationValidators()).toBeTruthy();
  });
});
